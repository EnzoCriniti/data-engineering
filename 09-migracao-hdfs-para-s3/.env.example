# Zona fria (object storage)
MINIO_USER=minioadmin
MINIO_PASSWORD=minioadmin
LAKE_BUCKET=nuvemstore-cold

# Porta do Metabase (BI) — única por capítulo p/ rodar vários ao mesmo tempo
METABASE_PORT=3008
